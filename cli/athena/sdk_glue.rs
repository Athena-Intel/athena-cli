//! Generated SDK client glue — bridges AppContext to the co-generated SDK.
//!
//! Auto-generated by @fern-api/cli-generator. Do not edit by hand.

use std::future::Future;
use std::pin::Pin;
use std::sync::Arc;

use fern_cli_sdk::error::CliError;
use fern_cli_sdk::openapi::AppContext;
use fern_cli_sdk::sdk_executor::{CliExecutor, SdkError, SdkRequestExecutor};

// ---------------------------------------------------------------------------
// Executor adapter: CliExecutor → SDK RequestExecutor
// ---------------------------------------------------------------------------

struct CliExecutorAdapter(Arc<CliExecutor>);

impl athena_sdk::RequestExecutor for CliExecutorAdapter {
    fn execute(
        &self,
        request: reqwest::Request,
    ) -> Pin<
        Box<
            dyn Future<Output = Result<reqwest::Response, Box<dyn std::error::Error + Send + Sync>>>
                + Send
                + '_,
        >,
    > {
        Box::pin(async move {
            SdkRequestExecutor::execute(&*self.0, request)
                .await
                .map_err(|e| Box::new(e) as Box<dyn std::error::Error + Send + Sync>)
        })
    }
}

// ---------------------------------------------------------------------------
// sdk_client — construct a fully-wired SDK root client
// ---------------------------------------------------------------------------

/// Build the SDK root client from the CLI's runtime context.
///
/// The returned client routes all HTTP through the CLI's executor, so
/// it inherits auth, retries, TLS, and global headers automatically.
pub fn sdk_client(ctx: &AppContext) -> athena_sdk::api::ApiClient {
    let executor = ctx.build_sdk_executor();
    let adapter = Arc::new(CliExecutorAdapter(executor));
    let config = athena_sdk::ClientConfig::default();
    let http_client = athena_sdk::HttpClient::with_executor(
        adapter as Arc<dyn athena_sdk::RequestExecutor>,
        config.clone(),
    );
    use athena_sdk::api::resources as r;
    athena_sdk::api::ApiClient {
        config,
        agents: r::AgentsClient {
            http_client: http_client.clone(),
            drive: r::agents::DriveClient { http_client: http_client.clone() },
            general: r::agents::GeneralClient { http_client: http_client.clone() },
            research: r::agents::ResearchClient { http_client: http_client.clone() },
            sql: r::agents::SqlClient { http_client: http_client.clone() },
        },
        aop: r::AopClient { http_client: http_client.clone() },
        assets: r::AssetsClient { http_client: http_client.clone() },
        databases: r::DatabasesClient { http_client: http_client.clone() },
        users: r::UsersClient { http_client: http_client.clone() },
        query: r::QueryClient { http_client: http_client.clone() },
        semantic_model: r::SemanticModelClient { http_client: http_client.clone() },
        threads: r::ThreadsClient { http_client: http_client.clone() },
        tools: r::ToolsClient {
            http_client: http_client.clone(),
            calendar: r::tools::CalendarClient { http_client: http_client.clone() },
            email: r::tools::EmailClient { http_client: http_client.clone() },
            sheets: r::tools::SheetsClient { http_client: http_client.clone() },
            structured_data_extractor: r::tools::StructuredDataExtractorClient {
                http_client: http_client.clone(),
            },
            tasks: r::tools::TasksClient { http_client: http_client.clone() },
        },
    }
}

// ---------------------------------------------------------------------------
// block_on — async SDK call → sync handler result
// ---------------------------------------------------------------------------

/// Execute an async SDK operation from a synchronous custom-command handler.
///
/// Bridges the SDK's `ApiError` into the CLI's `CliError` so `?` works
/// naturally in handler bodies.
pub fn block_on<F, T>(future: F) -> Result<T, CliError>
where
    F: Future<Output = Result<T, athena_sdk::ApiError>>,
{
    tokio::task::block_in_place(|| {
        let handle = tokio::runtime::Handle::current();
        handle.block_on(future).map_err(convert_api_error)
    })
}

fn convert_api_error(e: athena_sdk::ApiError) -> CliError {
    match e {
        athena_sdk::ApiError::Http { status, message } => CliError::Api {
            code: status,
            message,
            reason: http_status_reason(status).to_string(),
        },
        athena_sdk::ApiError::Network(err) => {
            CliError::Other(anyhow::anyhow!("SDK network error: {err}"))
        }
        athena_sdk::ApiError::Executor(boxed) => match boxed.downcast::<SdkError>() {
            Ok(sdk_error) => sdk_error.into_cli_error(),
            Err(other) => CliError::Other(anyhow::anyhow!("SDK executor error: {other}")),
        },
        other => CliError::Other(anyhow::anyhow!("SDK error: {other}")),
    }
}

fn http_status_reason(status: u16) -> &'static str {
    match status {
        400 => "badRequest",
        401 => "unauthorized",
        403 => "forbidden",
        404 => "notFound",
        408 => "requestTimeout",
        409 => "conflict",
        429 => "tooManyRequests",
        500 => "internalServerError",
        502 => "badGateway",
        503 => "serviceUnavailable",
        504 => "gatewayTimeout",
        _ => "httpError",
    }
}
